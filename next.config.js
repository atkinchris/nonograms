module.exports = {
  // Output as a static site.
  output: 'export',
  // Enable React Strict mode. https://reactjs.org/docs/strict-mode.html
  // This is the default from Next.js 13.4 for the app router, but not for pages.
  reactStrictMode: true,
  // Opt-in to the faster Next.js compiler.
  swcMinify: true,
}
